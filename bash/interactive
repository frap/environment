. ~/.config/shell/interactive

# +------------+
# | FZF        |
# +------------+
if quiet_which
then
    # Auto completion
    ssource "${HOMEBREW_PREFIX}/opt/fzf/shell/completion.bash"
    # git enhancements to fzf
    ssource "${XDG_CONFIG_HOME}/bash/fzf.zsh"
fi
